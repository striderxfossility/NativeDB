
public native struct inkWidgetPath {

  public final static native func IsValid(self: inkWidgetPath) -> Bool;

  public final static func Build(n0: CName) -> inkWidgetPath {
    let names: array<CName>;
    ArrayPush(names, n0);
    return BuildWidgetPath(names);
  }

  public final static func Build(n0: CName, n1: CName) -> inkWidgetPath {
    let names: array<CName>;
    ArrayPush(names, n0);
    ArrayPush(names, n1);
    return BuildWidgetPath(names);
  }

  public final static func Build(n0: CName, n1: CName, n2: CName) -> inkWidgetPath {
    let names: array<CName>;
    ArrayPush(names, n0);
    ArrayPush(names, n1);
    ArrayPush(names, n2);
    return BuildWidgetPath(names);
  }

  public final static func Build(n0: CName, n1: CName, n2: CName, n3: CName) -> inkWidgetPath {
    let names: array<CName>;
    ArrayPush(names, n0);
    ArrayPush(names, n1);
    ArrayPush(names, n2);
    ArrayPush(names, n3);
    return BuildWidgetPath(names);
  }

  public final static func Build(n0: CName, n1: CName, n2: CName, n3: CName, n4: CName) -> inkWidgetPath {
    let names: array<CName>;
    ArrayPush(names, n0);
    ArrayPush(names, n1);
    ArrayPush(names, n2);
    ArrayPush(names, n3);
    ArrayPush(names, n4);
    return BuildWidgetPath(names);
  }

  public final static func Build(n0: CName, n1: CName, n2: CName, n3: CName, n4: CName, n5: CName) -> inkWidgetPath {
    let names: array<CName>;
    ArrayPush(names, n0);
    ArrayPush(names, n1);
    ArrayPush(names, n2);
    ArrayPush(names, n3);
    ArrayPush(names, n4);
    ArrayPush(names, n5);
    return BuildWidgetPath(names);
  }

  public final static func Build(n0: CName, n1: CName, n2: CName, n3: CName, n4: CName, n5: CName, n6: CName) -> inkWidgetPath {
    let names: array<CName>;
    ArrayPush(names, n0);
    ArrayPush(names, n1);
    ArrayPush(names, n2);
    ArrayPush(names, n3);
    ArrayPush(names, n4);
    ArrayPush(names, n5);
    ArrayPush(names, n6);
    return BuildWidgetPath(names);
  }

  public final static func Build(n0: CName, n1: CName, n2: CName, n3: CName, n4: CName, n5: CName, n6: CName, n7: CName) -> inkWidgetPath {
    let names: array<CName>;
    ArrayPush(names, n0);
    ArrayPush(names, n1);
    ArrayPush(names, n2);
    ArrayPush(names, n3);
    ArrayPush(names, n4);
    ArrayPush(names, n5);
    ArrayPush(names, n6);
    ArrayPush(names, n7);
    return BuildWidgetPath(names);
  }
}
